# Single file Simple PHP Logger

Single file Simple PHP Logger, generates log in .txt file


#### :wrench: Features
- single file

### :books: How to use

```php
require "Logger.php";
$log = new Logger("log.txt");
$log->create("Hello log");
```
